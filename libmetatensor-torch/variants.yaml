c_stdlib:
  - if: osx # We don't build 32 bit anyway, and this is for arm and x64
    then: macosx_deployment_target
    else: sysroot
c_stdlib_version:
  - if: osx
    then: "10.15"
    else: "2.17"
